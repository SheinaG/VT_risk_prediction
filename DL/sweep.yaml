method: bayes
metric:
  goal: maximize
  name: best val AUROC
parameters:

  device:
    distribution: categorical
    values:
      - cuda
  gpu:
    distribution: categorical
    values:
      - "0"
  lr:
    min: 0.001
    max: 0.5

  model:
    distribution: categorical
    values:
      - Xception1D

  #  n_layers:
  #    distribution: int_uniform
  #    max: 10
  #    min: 1
  #
  #  ni:
  #    distribution: int_uniform
  #    max: 80
  #    min: 5
  #
  #  ks:
  #    distribution: int_uniform
  #    max: 99
  #    min: 1
  prob_augment_list:
    distribution: categorical
    values:
      - [ 0 ]
      - [ 0.1 ]
      - [ 0.3 ]
      - [ 0.5 ]
      - [ 0.8 ]







program: ./DL/Exp_overfit.py
